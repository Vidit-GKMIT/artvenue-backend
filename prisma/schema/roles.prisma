// Seeded Table

model roles {
  id         Int       @id @default(autoincrement())
  role       String    @unique @db.VarChar(15)
  created_at DateTime  @default(now()) @db.Timestamp()
  updated_at DateTime  @default(now()) @updatedAt @db.Timestamp()
  deleted_at DateTime? @db.Timestamp()

  users users[]
}